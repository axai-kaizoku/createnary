export default function DetailPageNum({ num }: { num: string }) {
	return (
		<div className="flex justify-center items-center px-6 py-3.5 rounded-lg text-3xl bg-primary-blue text-primary-white">
			{num}
		</div>
	);
}
